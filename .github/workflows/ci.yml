name: ci
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  required:
    name: ci / required
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: git diff --check
